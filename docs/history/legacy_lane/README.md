# Legacy-lane walkthroughs (archived 2026-09-03)

The two notebooks and the campaign spec here drove the retired SMIRKS screening lane (`scripts/run_pipeline.py`,
`scripts/run_campaign.py`). They do not run against the current tree and are kept as the record of that lane's
user surface. The live front door is `python scripts/maillard.py {compare,predict,explain,rank}`.
